fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
httpx==0.28.1
pytest==8.3.4

# Optional extensions - keep commented until a later stage needs them so the
# local PoC stays lightweight and dependency-free.
# langgraph
opensearch-py  # OpenSearch client library
# redis
# boto3
# deepeval
# opentelemetry-api
# opentelemetry-sdk
